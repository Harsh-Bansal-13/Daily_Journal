<%- include("partials/header"); -%>
    <h1>
        Home
    </h1>
    <p>
        <%= homecontent %>
    </p>
    <% arr.forEach(function(pst){%>
        <h1>
            <%= pst.title %>
        </h1>
        <p>
            <%= pst.content.substring(0,100)+" ..." %>
                <a href="/post/<%= pst.title%>">Read More</a>
        </p>

        <% }) %>
            <!-- <% for(var i=0;i < arr.length ; i++){%>
        <h1>
            <%= arr[i].title %>
        </h1>
        <p>
            <%= arr[i].content %>
        </p>

        <% } %> -->
            <%- include("partials/footer"); -%>